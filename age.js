// Function that takes age n years and returns the age in days.
function calcAge(age){
    return age * 365
}

// Test cases
console.log(calcAge(65)); // ➞ 23 725
console.log(calcAge(0)); // ➞ 0
console.log(calcAge(20)); // ➞ 7 300